import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import Header from '../../../components/header';

const AllFormat = () => {
  return (
    <View style={{flex: 1}}>
      <Header title="All Format" />
      <Text>AllFormat</Text>
    </View>
  );
};

export default AllFormat;

const styles = StyleSheet.create({});
